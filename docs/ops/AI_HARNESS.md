# AI Harness 運用リファレンス（Claude Code）

> **正本の分担**
> - Subagent Routing 表の正本: [`AGENTS.md`](../../AGENTS.md) / [`CLAUDE.md`](../../CLAUDE.md)（両ファイルは同内容で保つ）
> - 詳細リファレンス + 運用ガイド: 本ドキュメント
> - 再設計の一次記録（履歴）: [`docs/review/harness/2026-04-09-claude-code-harness-redesign.md`](../review/harness/2026-04-09-claude-code-harness-redesign.md)

**最終確認**: 2026-04-20

このドキュメントは career_compass の Claude Code ハーネス（agents / skills / hooks / MCP / commands）の詳細と運用手順をまとめたものです。`AGENTS.md` の routing 表とは意図的に別物で、routing 表は「どの変更対象をどの agent に委譲するか」だけを扱い、本ドキュメントは「各 agent / skill / hook / MCP サーバーが何をするか、どう追加・変更するか」を扱います。

---

## 0. このドキュメントの位置づけ

- **更新責任**: `.claude/` 配下（`agents/`, `skills/`, `hooks/`, `settings.json`）または `.mcp.json` を変更した PR では、本ドキュメントの該当節も同じ PR で更新する。
- **更新しないもの**: routing 表（= `AGENTS.md` / `CLAUDE.md` の Subagent Routing セクション）。本ドキュメントの 2 章 agents 表には「関連 skill」列があるため列構成が別物。
- **件数ドリフト対策**: 各一覧表の末尾に最終確認日を書く。件数点検は 7.4 節の検証コマンドで行う。
- **関連ドキュメント**:
  - [`docs/ops/AI_AGENT_PIPELINE.md`](./AI_AGENT_PIPELINE.md) — pipeline の思想と呼び出し方
  - [`docs/ops/CODEX_HARNESS.md`](./CODEX_HARNESS.md) — Codex custom agent / config / wrapper の運用リファレンス
  - `docs/ops/CURSOR_HARNESS.md` — Cursor rules / MCP / prompt template の運用リファレンス
  - [`docs/ops/AI_DEVELOPMENT_PRINCIPLES.md`](./AI_DEVELOPMENT_PRINCIPLES.md) — AI 継続開発での設計原則
  - [`docs/ops/CLI_GUARDRAILS.md`](./CLI_GUARDRAILS.md) — CLI 側のガードレール
  - [`docs/ops/SECURITY.md`](./SECURITY.md) — セキュリティベースライン

---

## 1. 全体像

### 1.1 レイヤ構成

```
AGENTS.md / CLAUDE.md  (Subagent Routing 正本)
        │
        ▼
.claude/agents/*.md     (13 agent 定義: model / tools / skills)
        │
        ▼
.claude/skills/*/SKILL.md  (35 skill: description マッチで自律起動)
.agents/skills/*/SKILL.md  (19 skill: canonical 一部 + 専門スキル)
        │
        ▼
.claude/hooks/*.sh      (13 top-level hook: Pre/PostToolUse, PermissionRequest, PostToolUseFailure, UserPromptSubmit, SessionStart, FileChanged, SubagentStart, Stop, SessionEnd)
.claude/hooks/lib/      (共通ライブラリ: skill-recommender.sh, e2e-functional-reminder.sh)
.claude/settings.json   (hook 配線)
.mcp.json               (Claude Code 用 project scope MCP: playwright + notion)
.cursor/mcp.json        (Cursor 用 project scope MCP: playwright + notion)
.codex/config.toml      (Codex 用 setup / verification / MCP)
~/.claude/settings.json (user scope MCP: context7、リポジトリ外)
.claude/commands/*.md   (3 command: workflow 一括実行)
```

### 1.2 canonical と mirror の関係

- **canonical source**: `private/agent-pipeline/skills/*.md`
- **generator**: `scripts/agent-pipeline/sync-pipeline.mjs`
- **mirror 対象（上書きされる）**:
  - `.claude/skills/{name}/SKILL.md`
  - `.codex/skills/{name}/SKILL.md`
  - `.codex/commands/{name}.md`
  - `.cursor/rules/{name}.mdc`
  - `.agents/skills/{name}/SKILL.md`（canonical を含む一部のみ）
- すべての生成ファイルの冒頭には `<!-- Generated by scripts/agent-pipeline/sync-pipeline.mjs. Edit private/agent-pipeline/skills/*.md instead. -->` バナーが入る。

**ルール**: `.claude/skills/`, `.codex/`, `.cursor/` は原則として手で編集しない。canonical を編集してから `node scripts/agent-pipeline/sync-pipeline.mjs` を実行し、mirror 差分を確認する。ただし 1.2.1 節に明記した non-canonical 例外は除く。

### 1.2.1 canonical 管轄外の skill

次の 10 skill は `sync-pipeline` の生成対象ではなく、`.agents/skills/` を実体とする **non-canonical skill** として手動管理する。

- 既存 symlink: `prompt-engineer`, `rag-engineer`, `hybrid-search-implementation`, `ui-ux-pro-max`, `vercel-react-best-practices`
- 追加 symlink: `ai-product`, `better-auth-best-practices`, `security-review`, `frontend-design`, `codex-review`

管理ルール:

- `.claude/skills/<name>` は `../../.agents/skills/<name>` への symlink として維持する
- 実体の更新は `.agents/skills/<name>/SKILL.md` を編集して行う
- これらの skill には generated banner が付かない。`private/agent-pipeline/skills/` へ移すまで sync-pipeline は触らない
- 新しい non-canonical skill を追加する場合は、本節と 7.3 の PR チェックリストを同じ PR で更新する
- `.cursor/rules/career-compass-core.mdc` は generator 管轄外の手動 `alwaysApply` ルールとして管理する

### 1.2.2 Claude/Codex 共通の AI functional E2E reminder

- `post-edit-dispatcher.sh` は AI 機能の実装入口を触ったとき、commit 前の `make test-e2e-functional-local-*` 実行を stderr に 1 セッション 1 回だけ案内する。
- canonical enforcement は repo-managed `.githooks/pre-commit` で、AI feature 変更時は localhost E2E の成功 manifest がない commit を block する。
- 初回導入時は `git config core.hooksPath .githooks` を実行して repo hook を有効化する。
- 判定ロジックは `.claude/hooks/lib/e2e-functional-reminder.sh` を正本にし、Claude と Codex の両 dispatcher から source する。
- 対象 feature は `es-review`, `gakuchika`, `motivation`, `interview`, `company-info-search`, `rag-ingest`, `selection-schedule`。
- `backend/app/utils/llm*.py`, `e2e/live-ai-*.spec.ts`, `scripts/ci/run-ai-live.sh`, `scripts/ci/run-e2e-functional.sh` などの共有変更では `make test-e2e-functional-local` を要求する。

### 1.3 user scope と project scope の境界

| スコープ | 設定ファイル | 対象 | リポジトリ管理 |
|---|---|---|---|
| project scope | `.mcp.json` | Claude Code 用 MCP（playwright, notion） | yes |
| project scope | `.cursor/mcp.json` | Cursor 用 MCP（playwright, notion） | yes |
| project scope | `.codex/config.toml` | Codex 用 setup / verification / MCP | yes |
| project scope | `.claude/settings.json` | hooks 配線 | yes |
| user scope | `~/.claude/settings.json` | context7 MCP | no（個人環境が正本） |

MCP 設定はツールごとの設定ファイルがそれぞれ正本であり、`.mcp.json` から自動生成はしない。本ドキュメントは差分管理のために「存在と用途」を一覧化する。user scope の設定はリポジトリには記録しない。

`.claude/settings.json` の `env` では Claude Code の Bash timeout を project scope で上書きしており、`BASH_DEFAULT_TIMEOUT_MS=3600000` と `BASH_MAX_TIMEOUT_MS=3600000` を設定する。これにより、Claude から `bash scripts/codex/delegate.sh ...` のような長時間委譲コマンドを実行しても、デフォルト 2 分 / 最大 10 分ではなく 60 分まで許可する。

---

## 2. サブエージェント リファレンス

### 2.1 一覧

**最終確認**: 2026-04-12 時点で 13 agents（opus 11 + sonnet 2）。定義は `.claude/agents/<name>.md`。

| name | model | 主な対象 / 責務 | 関連 skill |
|---|---|---|---|
| architect | opus | auth / billing / calendar / AI / RAG の境界変更、API/backend/schema をまたぐ新機能、アーキテクチャゲート、PRD / RFC | architecture-gate, improve-architecture, write-prd, prd-to-issues |
| prompt-engineer | opus | `backend/app/prompts/**`, `backend/app/utils/llm.py`, LLM 出力品質 / A/B | prompt-engineer, ai-writing-auditor, llm-architect, ai-product |
| security-auditor | opus | `src/lib/auth/**`, `src/lib/csrf.ts`, `src/app/api/webhooks/stripe/**`, `src/lib/stripe/**`, OWASP | security-auditor, payment-integration, better-auth-best-practices, security-review |
| code-reviewer | opus | PR 前レビュー、500 行超ファイル、dead code、リファクタ計画 | code-reviewer, refactoring-specialist |
| fastapi-developer | opus | `backend/app/routers/**`, `backend/app/main.py`, SSE streaming, Pydantic v2 | fastapi-developer |
| nextjs-developer | opus | `src/app/**/page.tsx` のロジック, `src/app/api/**/route.ts`, `src/hooks/`, `src/lib/` | nextjs-developer, vercel-react-best-practices |
| ui-designer | opus | `src/components/**`, `src/app/(marketing)/**`, `src/app/**/(page\|layout\|loading).tsx` のビジュアル | ui-ux-pro-max, frontend-design |
| database-engineer | opus | `src/lib/db/schema.ts`, `drizzle_pg/`, マイグレーション, インデックス | postgres-pro, database-optimizer |
| rag-engineer | opus | `backend/app/utils/(vector_store\|hybrid_search\|embeddings\|text_chunker\|content_classifier).py` | rag-engineer, hybrid-search-implementation, ai-product |
| search-quality-engineer | opus | `backend/app/utils/(bm25_store\|reranker\|japanese_tokenizer\|web_search).py`, 検索品質改善 | improve-search, nlp-engineer |
| test-automator | sonnet | `e2e/**`, `backend/tests/**`, `src/**/*.test.ts`, AI Live | test-automator, tdd |
| release-engineer | sonnet | `scripts/release/**`, `Makefile` の release targets, provider CLI, secrets sync | release-automation, railway-ops, supabase-ops |
| product-strategist | opus | `src/app/(marketing)/**` の戦略、UX 調査、競合、SEO、無料ツール戦略 | ux-researcher, competitive-analyst, seo-specialist |

> 対象パスは `AGENTS.md` の Subagent Routing 表と 1 対 1 対応している。変更対象 → 委譲先のマッピング正本は `AGENTS.md` / `CLAUDE.md` を参照。

### 2.2 opus と sonnet の使い分け方針

- **opus**（11 agents）: 設計判断、品質審査、セキュリティ監査、プロンプト最適化、実装、検索品質など「間違うとコストが高い」領域。
  - architect, prompt-engineer, security-auditor, code-reviewer, fastapi-developer, nextjs-developer, ui-designer, database-engineer, rag-engineer, search-quality-engineer, product-strategist
- **sonnet**（2 agents）: テスト自動化、リリース運用など「反復速度が重要」な領域。
  - test-automator, release-engineer

### 2.3 委譲判断フロー

1. `AGENTS.md` の Subagent Routing 表で対象パスを検索し、マッチすればその agent に委譲する。
2. 複数領域をまたぐ（2 ファイル以上 + 責務境界を跨ぐ）場合は `architect` を起点にし、architecture-gate skill の結果に従って個別 agent に分解する。
3. PR 前の品質審査は `code-reviewer`、セキュリティ境界の変更は `security-auditor` を追加で回す。
4. docs-only / test-only / 局所的な文言修正 / 明らかな局所バグ修正は委譲を省略してよい（`AGENTS.md` の方針）。

### 2.4 サブエージェントの追加・変更手順

1. `.claude/agents/<name>.md` を作成。frontmatter に `name`, `description`, `model`, `tools` を書く（既存 13 ファイルの形式に合わせる）。
2. `AGENTS.md` と `CLAUDE.md` の Subagent Routing 表に行を追加する（両ファイルを byte-identical に保つ）。
3. 本ドキュメント 2.1 の表にも行を追加する。`関連 skill` 列は実在する skill のみ記載する。
4. session-orientation hook が `ls .claude/agents` から active subagents 一覧を生成しているため、Claude Code を再起動すれば新 agent が注入される。

---

## 3. スキル リファレンス

### 3.1 `.claude/skills/*/SKILL.md`

**最終確認**: 2026-04-19 時点で 41 skill。canonical は `private/agent-pipeline/skills/*.md`、一部は `.agents/skills/` からの symlink / コピー。

| name | 1 行要約 | 主な呼び出し agent |
|---|---|---|
| architecture-gate | omm とコードで次の変更が安全に追加できるか `PASS` / `PASS_WITH_REFACTOR` / `BLOCK` で判定 | architect |
| improve-architecture | 構造的な弱点をレビューし、選択肢を比較して RFC を書く | architect |
| write-prd | validated design を PRD に変換（scope / 制約 / 受入基準） | architect |
| prd-to-issues | PRD を依存関係・リスク・受入基準つきの thin vertical slice に分解 | architect |
| prompt-engineer | ES 添削 / 下書き生成プロンプトの設計・最適化・A/B | prompt-engineer |
| ai-writing-auditor | ES 添削・下書き生成出力の AI 臭を検出し、自然な日本語 ES へ改善 | prompt-engineer |
| empirical-prompt-tuning | subagent 実行＋メトリクス比較でプロンプト変更の効果を客観検証し反復改善 | prompt-engineer |
| llm-architect | マルチモデル（Claude / GPT / Gemini）ルーティング、RAG + LLM 統合、コスト最適化 | prompt-engineer |
| ai-product | RAG + LLM integration、AI UX、コスト最適化の実践知 | prompt-engineer, rag-engineer |
| code-reviewer | TS + Python の大規模ファイルレビュー、OWASP 準拠確認 | code-reviewer |
| codex-review | Professional code review と CHANGELOG 生成 | 直接参照は任意（レビュー補助） |
| refactoring-specialist | 大規模ファイル分割、責務分離、tdd との連携 | code-reviewer |
| fastapi-developer | backend/app の開発、SSE、Pydantic v2、検索パイプライン | fastapi-developer |
| nextjs-developer | App Router / Server Components / Core Web Vitals 最適化 | nextjs-developer |
| vercel-react-best-practices | Vercel Engineering の React/Next.js 最適化ルール 57 個 | nextjs-developer |
| ui-ux-pro-max | UI/UX デザイン、50 styles / 21 palettes / 9 stacks | ui-designer |
| frontend-design | 個性ある production-grade frontend UI の設計原則 | ui-designer |
| database-optimizer | Drizzle の N+1 検出、コネクションプール、マイグレーション設計 | database-engineer |
| postgres-pro | Supabase PostgreSQL クエリ最適化、インデックス、Drizzle パフォーマンス | database-engineer |
| rag-engineer | Embeddings / vector DB / chunking / retrieval 最適化 | rag-engineer |
| hybrid-search-implementation | ベクトル検索 + キーワード検索の統合 | rag-engineer |
| improve-search | 企業検索（hybrid / legacy）の精度・網羅性を自律改善（テスト履歴分析 → 仮説 → 実装 → 評価） | search-quality-engineer |
| nlp-engineer | BM25 + Cross-Encoder reranking、テキスト分類、embeddings 最適化 | search-quality-engineer |
| security-auditor | CSRF、ゲスト認証、API 認証、OWASP 準拠の体系的監査 | security-auditor |
| payment-integration | Stripe webhook、サブスクリプション管理、クレジット消費の整合性 | security-auditor |
| better-auth-best-practices | Better Auth + Google OAuth のベストプラクティス | security-auditor |
| security-review | auth / input validation / secrets / APIs の広範セキュリティレビュー | security-auditor |
| test-automator | Vitest + Playwright + pytest 自動化、AI Live テスト拡張、CI 改善 | test-automator |
| tdd | 失敗テスト → 最小実装 → リファクタの順で進める | test-automator |
| release-automation | develop preflight → staged commit → staging 検証 → develop→main 昇格 → production Playwright | release-engineer |
| railway-ops | Railway staging/production の env sync / health check / custom domain | release-engineer |
| supabase-ops | Supabase shared project bootstrap check と secret sync | release-engineer |
| competitive-analyst | 就活支援市場の競合分析・差別化戦略 | product-strategist |
| ux-researcher | ターゲット（就活塾に行かない学生）のユーザビリティ調査 | product-strategist |
| seo-specialist | LP・無料ツール・テンプレートの検索流入最適化 | product-strategist |
| x-mentor | X (Twitter) 向けの投稿作成・添削・フック改善 | 直接参照は任意（発信系作業） |
| codex-delegation-workflow | Codex CLI への委譲判断基準とオーケストレーション（plan_review / implementation / post_review） | Claude Code オーケストレーション |

### 3.2 `.agents/skills/*/SKILL.md`

**最終確認**: 2026-04-12 時点で 19 skill。canonical の一部 + `.claude/skills/` にも展開される skill と、`.agents/skills/` のみに存在する専門スキルが混在。

| name | 1 行要約 | `.claude/skills/` 同名あり |
|---|---|---|
| prompt-engineer | ES 添削・下書き生成プロンプトの設計・最適化・A/B | yes |
| release-automation | develop → staging → main → production の標準フロー | yes |
| railway-ops | Railway staging/production 運用 | yes |
| supabase-ops | Supabase shared project 運用 | yes |
| rag-engineer | Embeddings / vector DB / chunking / retrieval 最適化 | yes |
| hybrid-search-implementation | ベクトル検索 + キーワード検索の統合 | yes |
| ui-ux-pro-max | UI/UX デザイン、50 styles / 21 palettes / 9 stacks | yes |
| vercel-react-best-practices | Vercel Engineering の React/Next.js 最適化ルール 57 個 | yes |
| ai-product | RAG + LLM integration、AI UX、コスト最適化 | yes（symlink） |
| better-auth-best-practices | Better Auth + Google OAuth のベストプラクティス | yes（symlink） |
| security-review | auth / input validation / secrets / APIs の広範レビュー | yes（symlink） |
| frontend-design | 個性ある production-grade frontend UI の設計原則 | yes（symlink） |
| codex-review | Professional code review と CHANGELOG 生成 | yes（symlink） |
| x-mentor | X (Twitter) 投稿作成・添削 | yes |
| rag-implementation | RAG システム構築（vector DB + semantic search） | no（`.agents/` 専門） |
| similarity-search-patterns | 類似検索パターン（semantic search / RAG / 推薦） | no |
| senior-ml-engineer | ML 本番化、MLOps、LLM fine-tuning | no |
| database-design | Schema 設計、indexing、ORM 選択、serverless DB | no |
| audit-website | squirrelscan CLI で SEO / 性能 / セキュリティを監査 | no |

### 3.3 skill / command / 自然言語の切り分け

| 呼び出し方 | 特徴 | 例 |
|---|---|---|
| skill（自律起動） | description マッチで Claude 側が呼び出す。pipeline の各ステップはこちら | `architecture-gate`, `write-prd`, `tdd` |
| command | 逐次ステップのワークフローを一括実行。`.claude/commands/*.md` の 3 種のみ | `deploy-production`, `reset-changes`, `update-docs` |
| 自然言語 + routing 表 | 対象パスへのファイル編集を起点に agent が自動委譲 | `backend/app/prompts/**` → prompt-engineer |

### 3.4 スキルの追加・変更手順（canonical 経由）

```bash
# 1. canonical を編集する
$EDITOR private/agent-pipeline/skills/<name>.md

# 2. generator を実行（全 mirror を上書き）
node scripts/agent-pipeline/sync-pipeline.mjs

# 3. 差分を確認
git status .claude/skills/ .codex/ .cursor/ .agents/skills/

# 4. sync-pipeline のテスト
npm run test:agent-pipeline
```

**禁止事項**: `.claude/skills/*.md`, `.codex/**/*.md`, generator 管轄の `.cursor/rules/*.mdc` を手で編集する。次回の sync で巻き戻る。例外は 1.2.1 の non-canonical skill と `.cursor/rules/career-compass-core.mdc`。

### 3.5 sync-pipeline の上書き対象 path 一覧

`scripts/agent-pipeline/sync-pipeline.mjs` は以下に同じ skill 定義を複製する。各形式ごとに frontmatter と banner を付け替える。

| path | 用途 | render 関数 |
|---|---|---|
| `.claude/skills/{name}/SKILL.md` | Claude Code 用 skill | `renderClaudeSkill()` |
| `.codex/skills/{name}/SKILL.md` | Codex IDE 用 skill | `renderCodexSkill()` |
| `.codex/commands/{name}.md` | Codex コマンド定義 | `renderCodexCommand()` |
| `.cursor/rules/{name}.mdc` | Cursor IDE ルール | `renderCursorRule()` |

> 2026-04-09 に `.claude/commands/` への出力は廃止された。pipeline 系 command は skill に一本化されている。

---

## 4. Commands リファレンス

### 4.1 現行コマンド

**最終確認**: 2026-04-19 時点で 11 コマンド。定義は `.claude/commands/<name>.md`。

| name | 目的 |
|---|---|
| deploy-production | 標準 release automation。`make ops-release-check` → `make deploy-stage-all`（必要なら）→ `make deploy`。直接的な provider CLI 呼び出しは禁止 |
| reset-changes | 直前の変更を作業前状態に戻す。`uncommitted` / `last-commit` / `last-commit-hard` / `file <path>` のモードを選択可。reflog からの復元手順も案内 |
| update-docs | ドキュメントを最新の実装状態に同期。対象はファイルパスまたはカテゴリ（`progress`, `features`, `architecture`, `setup`, `all`）で指定 |
| grill-me | 実装前に要件 / 設計を詰め切るまで Claude が質問する対話モード |
| write-prd | grill-me の成果を PRD に落とし込み `docs/prd/` に保存 |
| prd-to-issues | PRD を thin vertical slice の issue 群に分解して `docs/issues/` に保存 |
| tdd | Red-Green-Refactor と boundary test から入る TDD 実装 |
| improve-architecture | リポジトリを走査して RFC を `docs/rfc/` に生成 |
| codex-plan-review | Codex CLI (GPT-5.4) に設計/計画の read-only レビューを委譲。`scripts/codex/delegate.sh plan_review` を起動 |
| codex-implement | Codex CLI に実装タスクを委譲（workspace-write sandbox）。`scripts/codex/delegate.sh implementation` を起動 |
| codex-post-review | Codex CLI に uncommitted changes のコードレビューを委譲。`codex review --uncommitted` を使用 |

### 4.2 pipeline 系 command の変遷（履歴）

2026-04-09 の harness 再設計で一度 skill に一本化したが、CLI 同義語としての起動性を優先して `architecture-gate` を除く 5 系統（`grill-me`, `write-prd`, `prd-to-issues`, `tdd`, `improve-architecture`）は command としても再配置されている。実装本体は `.agents/skills/<name>/SKILL.md` が正本で、command は委譲 shim にとどめる。

背景の詳細は [`docs/review/harness/2026-04-09-claude-code-harness-redesign.md`](../review/harness/2026-04-09-claude-code-harness-redesign.md) を参照。

### 4.3 command を新設すべきケース

次の条件をすべて満たす時だけ command を作る。通常は skill で十分。

- 逐次ステップのワークフローで順序を強制する必要がある
- 1 プロンプトで description マッチだけでは暴発を避けられない
- 実行可能な code block として Claude に手順を順番に踏ませたい（skill は description マッチで呼ばれるだけで、step 順序は自律判断）

---

## 5. Hooks リファレンス

### 5.1 一覧

**最終確認**: 2026-04-19 時点で 13 top-level hook + 共通ライブラリ 1。配線は `.claude/settings.json`、実体は `.claude/hooks/*.sh`、共通ヘルパーは `.claude/hooks/lib/skill-recommender.sh`（hook 本体が `source` する）。

| name | event | matcher | 停止動作 | 目的 |
|---|---|---|---|---|
| `ui-preflight-reminder.sh` | PreToolUse | `Edit\|Write` | exit 0（警告のみ） | UI ファイル編集前に preflight チェック手順を喚起 |
| `git-push-guard.sh` | PreToolUse | `Bash` | force push は exit 2（block）、main/develop push は exit 0（警告） | 危険な git push を止め、release 正本の make deploy 経由を案内 |
| `secrets-guard.sh` | PreToolUse | `Read\|Bash` | exit 2（block） | `codex-company/.secrets/` 配下への直接アクセスを止める |
| `post-edit-dispatcher.sh` | PostToolUse | `Edit\|Write` | exit 0（提案のみ） | hotspot / 500 行超 / cross-dir / prompt / 保守性 / schema / test の 7 系統リマインダを path ごとに集約（`lib/skill-recommender.sh` を source） |
| `permission-request-guard.sh` | PermissionRequest | `Read\|Bash` | `hookSpecificOutput.decision.behavior = "deny"` で block | `codex-company/.secrets/` の Read / grep 系と `git push --force` 系を user 確認プロンプト前に deny |
| `post-tool-failure-triage.sh` | PostToolUseFailure | `Bash\|Read\|WebFetch` | exit 0（`additionalContext` 注入） | sandbox / network / auth / path typo 系の失敗に対して次アクションのヒントを LLM へ注入 |
| `user-prompt-submit-router.sh` | UserPromptSubmit | matcher なし | exit 0（提案のみ） | ユーザープロンプトから routing 候補 subagent を提案 |
| `session-orientation.sh` | SessionStart | `startup\|resume` | exit 0（stdout 注入） | branch / git status / 直近 5 コミット / active subagents を session 開始時に context 注入 |
| `file-changed-lint.sh` | FileChanged | `package.json\|tsconfig.json\|...` | exit 0（提案のみ） | 高シグナルな JS/TS 系ファイル変更時に `npm run lint -- <path>` を自動実行 |
| `subagent-start-log.sh` | SubagentStart | matcher なし | exit 0（ログのみ） | subagent 起動を `docs/ops/agent-usage.log` に記録 |
| `stop-summary.sh` | Stop | matcher なし | exit 0（要約のみ） | `git status --short` 要約に加え、変更ファイル数 ≥10 / 変更行 ≥500 / hotspot 変更で maintainability-review 推奨（`lib/skill-recommender.sh` を source） |
| `session-end-cleanup.sh` | SessionEnd | matcher なし | exit 0（cleanup のみ） | edit-count / cross-dir flag 等セッション状態ファイルの削除と終了理由ログ |

#### 5.1.1 共通ライブラリ `lib/skill-recommender.sh`

`post-edit-dispatcher.sh` と `stop-summary.sh` が共通で使う軽量ヘルパー群。これを切り出したことで「hotspot 判定」「500 行閾値」「セッション状態ファイルの置き場」を両 hook で重複させずに済む。

提供関数:

- `is_hotspot_path "<path>"` — `docs/ops/AI_DEVELOPMENT_PRINCIPLES.md` 列挙の hotspot（`backend/app/routers/company_info.py`, `es_review.py`, `backend/app/utils/llm.py`, `src/components/companies/CorporateInfoSection.tsx`, `src/components/es/ReviewPanel.tsx`, `src/hooks/useESReview.ts`, `src/lib/server/app-loaders.ts`）と一致すれば 0
- `file_line_count "<path>"` — 行数を stdout に
- `is_oversized_file "<path>" [threshold]` — 既定 500 行超なら 0
- `skill_session_state_dir` — `$HOME/.claude/sessions/career_compass` を mkdir し出力
- `skill_state_file <session> <name>` / `skill_increment_counter <file>` / `skill_touch_flag <file>` — 状態ファイル操作
- `codex_delegation_dir` — `.claude/state/codex-handoffs/` を mkdir し出力
- `is_codex_post_review_candidate <files> <lines> <hotspot>` — Codex post_review 推奨判定（files ≥ 10 / lines ≥ 500 / hotspot 変更）
- `latest_codex_handoff_result` — 最新の handoff result.md パスを出力

hotspot 一覧の同期は `docs/ops/AI_DEVELOPMENT_PRINCIPLES.md` を正本とする。hotspot を増減した時は両者を同時に更新する。

### 5.2 `.claude/settings.json` の構造（現行）

```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Edit|Write", "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/ui-preflight-reminder.sh" }] },
      { "matcher": "Bash",       "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/git-push-guard.sh" }] },
      { "matcher": "Read|Bash",  "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/secrets-guard.sh" }] }
    ],
    "PostToolUse": [
      { "matcher": "Edit|Write", "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/post-edit-dispatcher.sh" }] }
    ],
    "PermissionRequest": [
      { "matcher": "Read|Bash",         "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/permission-request-guard.sh" }] }
    ],
    "PostToolUseFailure": [
      { "matcher": "Bash|Read|WebFetch", "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/post-tool-failure-triage.sh" }] }
    ],
    "UserPromptSubmit": [
      { "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/user-prompt-submit-router.sh" }] }
    ],
    "SessionStart": [
      { "matcher": "startup|resume", "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/session-orientation.sh" }] }
    ],
    "FileChanged": [
      { "matcher": "package.json|tsconfig.json|eslint.config.mjs|playwright.config.ts|playwright.live.config.ts|next.config.ts|next.config.js|src/lib/db/schema.ts", "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/file-changed-lint.sh" }] }
    ],
    "SubagentStart": [
      { "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/subagent-start-log.sh" }] }
    ],
    "Stop": [
      { "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/stop-summary.sh" }] }
    ],
    "SessionEnd": [
      { "hooks": [{ "type": "command", "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/session-end-cleanup.sh" }] }
    ]
  }
}
```

matcher は通常イベントではツール名などへの regex として使う。`FileChanged` だけは literal filename watch list と発火フィルタを兼ねるため、対象を明示的に列挙する（公式仕様）。

### 5.3 各 hook の詳細

#### 5.3.1 `ui-preflight-reminder.sh`（PreToolUse, Edit|Write）

- **対象 path**: `src/components/**`, `src/app/**/(page|layout|loading).tsx`, `src/components/skeletons/**` など UI ファイル
- **動作**: exit 0（警告のみ）。標準エラー出力に UI Change Workflow のチェックリストを出す
- **推奨手順**: `npm run ui:preflight -- <route> --surface=marketing|product` → `npm run lint:ui:guardrails` → `npm run test:ui:review -- <route>`
- **相互参照**: [`docs/architecture/FRONTEND_UI_GUIDELINES.md`](../architecture/FRONTEND_UI_GUIDELINES.md), [`docs/testing/UI_PLAYWRIGHT_VERIFICATION.md`](../testing/UI_PLAYWRIGHT_VERIFICATION.md), `AGENTS.md` の UI Change Workflow 節

#### 5.3.2 `git-push-guard.sh`（PreToolUse, Bash）

- **対象コマンド**: `git push` を含む Bash コマンド全般
- **block（exit 2）**: `git push --force`, `git push -f`, `git push --force-with-lease` を含むコマンド
- **警告（exit 0）**: `git push ... main` または `git push ... develop`
- **スルー（exit 0）**: それ以外
- **release 正本**: `make deploy` / `make ops-release-check` → `make deploy-stage-all` / `scripts/release/release-career-compass.sh`
- **相互参照**: [`docs/ops/CLI_GUARDRAILS.md`](./CLI_GUARDRAILS.md), [`docs/release/PRODUCTION.md`](../release/PRODUCTION.md)

#### 5.3.3 `secrets-guard.sh`（PreToolUse, Read|Bash）

- **block（exit 2）**:
  - Read tool で `codex-company/.secrets/` を含む path を開こうとする
  - Bash tool で `cat|head|tail|less|more|bat|sed|awk|grep|rg` 等の読み取り系コマンドが `codex-company/.secrets/` を対象にする
- **許可される操作**: `zsh scripts/release/sync-career-compass-secrets.sh --check`
- **相互参照**: [`docs/ops/SECURITY.md`](./SECURITY.md), [`docs/release/ENV_REFERENCE.md`](../release/ENV_REFERENCE.md), `CLAUDE.md` の API / Error Handling Rules 節

#### 5.3.4 `post-edit-dispatcher.sh`（PostToolUse, Edit|Write）

- **入力**: `file_path` と `session_id` を受け取り、1 回の hook 実行で複数の path-aware reminder を判定する
- **共通ライブラリ**: `lib/skill-recommender.sh` を source し `is_hotspot_path` / `is_oversized_file` / `skill_touch_flag` 等を利用
- **内包するリマインダ**（判定順序）:
  1. **hotspot ヒット** — 編集 path が `HOTSPOT_FILES` にマッチしたら refactoring-specialist / maintainability-review / code-reviewer skill を推奨
  2. **500 行超ファイルへの追記** — `.ts / .tsx / .py` かつ既存ファイルが 500 行超なら責務分離の検討（refactoring-specialist / architecture-gate）
  3. **cross-dir 変更検出** — 同セッションで `src/app/api/*` と `backend/app/*` の両方を触ったら境界整理（architect skill）を提案。flag は `$HOME/.claude/sessions/career_compass/edited-{next-api,fastapi,cross-notified}-{SESSION_ID}` で保持
  4. prompt / LLM ファイル（`backend/app/prompts/*`, `backend/app/utils/llm*.py`）変更時の eval + 参考 ES 漏洩チェック
  5. `.ts`, `.tsx`, `.py` 編集 5 回ごとの maintainability チェック（`/simplify` skill / code-reviewer agent）
  6. `src/lib/db/schema.ts` 変更時の migration 生成 / SQL レビュー / database-engineer 委譲
  7. Vitest / Playwright / pytest テストファイル変更時の実行コマンド提案（path をそのまま埋め込む）
- **発火頻度**: maintainability 用カウンタは `$HOME/.claude/sessions/career_compass/edit-count-{SESSION_ID}` で管理する
- **動作**: exit 0（提案のみ）。複数条件に同時一致する場合は同一実行内で順番にメッセージを出す（Vitest / Playwright 専用メッセージは `exit 0` で短絡）

#### 5.3.5 `session-orientation.sh`（SessionStart, startup|resume）

- **動作**: exit 0。stdout が session 開始時の context に注入される（公式仕様）
- **注入内容**: branch 名、`git status --short` の先頭 20 行、`git log --oneline -5`、`ls .claude/agents` から生成した active subagents 一覧
- **運用上の効果**: 新しい agent を `.claude/agents/` に追加すると、次の session から自動で routing 候補として提示される

#### 5.3.6 `file-changed-lint.sh`（FileChanged）

- **対象 watch list**: `package.json`, `tsconfig.json`, `eslint.config.mjs`, `playwright.config.ts`, `playwright.live.config.ts`, `next.config.ts`, `next.config.js`, `src/lib/db/schema.ts`
- **動作**: exit 0（通知のみ）。対象ファイルが JS/TS 系なら `npm run lint -- <relative-path>` を実行し、結果を stderr に出す
- **制約**: Claude Code の `FileChanged` は literal filename watch のため、repo 全体の全ファイル監視ではなく高シグナルなファイルに限定している

#### 5.3.7 `subagent-start-log.sh`（SubagentStart）

- **入力**: `agent_type`, `agent_id`, `session_id`
- **動作**: exit 0。`docs/ops/agent-usage.log` に UTC timestamp 付きで追記する
- **用途**: subagent 利用頻度とコスト傾向の事後レビュー

#### 5.3.8 `stop-summary.sh`（Stop）

- **動作**: exit 0。branch 名と `git status --short` の要約を stderr に出す
- **共通ライブラリ**: `lib/skill-recommender.sh` を source（hotspot 判定のみ利用）
- **大規模変更検出**（2026-04-16 追加）: 以下いずれかに該当すると maintainability-review skill / improvement-plan skill / refactoring-specialist skill を推奨
  - 変更ファイル数 ≥ 10（`git diff --numstat HEAD` + untracked の合算）
  - 変更行数 ≥ 500（追加 + 削除、binary は除外）
  - hotspot（`HOTSPOT_FILES`）の変更を 1 件でも含む
- **用途**: 応答完了ごとに dirty 状態を可視化し、未整理差分の持ち越しを減らす。大規模変更の見逃しを stop 時に機械的に検出する

#### 5.3.9 `permission-request-guard.sh`（PermissionRequest, Read|Bash）

- **入力**: `tool_name`, `tool_input`（公式 `PermissionRequestHookInput` 準拠）
- **出力**: `hookSpecificOutput.decision.behavior = "deny"` を JSON で stdout に返すと user 確認プロンプトを出さずに block（`interrupt: true` で即終了）
- **block 条件**:
  - Read tool で `codex-company/.secrets/` を対象とする場合
  - Bash tool で `git push --force` / `-f` / `--force-with-lease` を含む場合
  - Bash tool で `cat|head|tail|less|more|bat|sed|awk|grep|rg` 系が `codex-company/.secrets/` を読もうとする場合
- **PreToolUse 系との役割分担**: `secrets-guard.sh` / `git-push-guard.sh`（PreToolUse, exit 2 で block）を抜けた後に user へ確認プロンプトが出る導線を、`PermissionRequest` イベントで更に 1 段 deny する defense-in-depth。user が誤って allow を押すリスクを減らす

#### 5.3.10 `post-tool-failure-triage.sh`（PostToolUseFailure, Bash|Read|WebFetch）

- **入力**: `tool_name`, `tool_input`, `error`（公式 `PostToolUseFailureHookInput` 準拠）
- **出力**: `hookSpecificOutput.additionalContext` に次アクションのヒントを JSON で stdout 返却（LLM が次ターンで参照）
- **判定ルール**:
  - Bash × `sandbox / permission / operation not permitted / not permitted` → escalated permission を要求する判断を促す
  - Bash × `ENOTFOUND / EAI_AGAIN / network / temporary failure` → network 起因として escalated permission を検討
  - Bash × `npm run test / pytest / vitest / playwright` の失敗 → 環境問題と決めつけず failing assertion を確認させる
  - Read × `no such file / not found` → path typo / ファイル移動を疑い `rg --files` での再確認
  - WebFetch × `403 / 401 / forbidden / unauthorized` → 認証付き URL / bot block の可能性。Playwright / MCP / shareable URL へのフォールバック
- **動作**: 該当ルールにヒットしない場合は exit 0 で silent pass

#### 5.3.11 `user-prompt-submit-router.sh`（UserPromptSubmit）

- **動作**: ユーザープロンプトの内容から routing 表に基づく subagent 候補を提案（skill-recommender とは独立の router）
- **停止動作**: exit 0

#### 5.3.12 `session-end-cleanup.sh`（SessionEnd）

- **対象**: `$HOME/.claude/sessions/career_compass/edit-count-{SESSION_ID}`, および `edited-next-api-*` / `edited-fastapi-*` / `cross-notified-*` flag
- **動作**: exit 0。session 単位の maintainability counter と cross-dir flag を削除し、終了理由を stderr に出す
- **制約**: `SessionEnd` はデフォルト timeout が短いため、重い処理は入れない

### 5.4 dry-run 検証手順

```bash
# UI ファイル編集前リマインダが発火するか
echo '{"tool_input":{"file_path":"/tmp/src/components/Foo.tsx"}}' \
  | .claude/hooks/ui-preflight-reminder.sh; echo "exit=$?"
# 期待: stderr にチェックリスト、exit=0

# git push --force が block されるか
echo '{"tool_input":{"command":"git push --force origin main"}}' \
  | .claude/hooks/git-push-guard.sh; echo "exit=$?"
# 期待: stderr に block メッセージ、exit=2

# main への push が警告のみで通るか
echo '{"tool_input":{"command":"git push origin main"}}' \
  | .claude/hooks/git-push-guard.sh; echo "exit=$?"
# 期待: stderr に警告、exit=0

# secrets Read が block されるか
echo '{"tool_name":"Read","tool_input":{"file_path":"codex-company/.secrets/.env"}}' \
  | .claude/hooks/secrets-guard.sh; echo "exit=$?"
# 期待: stderr に block メッセージ、exit=2

# secrets の cat が block されるか
echo '{"tool_name":"Bash","tool_input":{"command":"cat codex-company/.secrets/.env"}}' \
  | .claude/hooks/secrets-guard.sh; echo "exit=$?"
# 期待: stderr に block メッセージ、exit=2

# schema.ts 変更リマインダが発火するか
echo '{"tool_input":{"file_path":"src/lib/db/schema.ts"}}' \
  | .claude/hooks/post-edit-dispatcher.sh; echo "exit=$?"
# 期待: stderr に migration リマインダ、exit=0

# pytest テスト変更リマインダが発火するか（多段パス）
echo '{"tool_input":{"file_path":"backend/tests/es_review/test_reference_es_quality.py"}}' \
  | .claude/hooks/post-edit-dispatcher.sh; echo "exit=$?"
# 期待: stderr にファイルパス埋め込みのテスト実行提案、exit=0

# FileChanged lint が走るか
echo '{"file_path":"'"$PWD"'/package.json"}' \
  | .claude/hooks/file-changed-lint.sh; echo "exit=$?"
# 期待: stderr に lint 実行結果、exit=0

# SessionEnd cleanup が走るか
echo '{"session_id":"dry-run","reason":"other"}' \
  | .claude/hooks/session-end-cleanup.sh; echo "exit=$?"
# 期待: stderr に cleanup 結果、exit=0
```

### 5.5 新しい hook を追加する手順

1. `.claude/hooks/<name>.sh` を作成し、`chmod +x` で実行権限を付与する
2. 入力は stdin から JSON で来る。`jq -r '.tool_input.*'` または `.tool_name` を使って判定する
3. 警告は stderr + exit 0、block は stderr + exit 2
4. `.claude/settings.json` の該当 event 配下に `matcher` と `command` を追加する
5. 5.1 の一覧表と 5.3 の詳細節に追記する
6. 5.4 の手順で dry-run する

---

## 6. MCP サーバー リファレンス

### 6.1 project scope MCP 設定

**現行**: Claude Code 用 `.mcp.json`、Cursor 用 `.cursor/mcp.json`、Codex 用 `.codex/config.toml` の 3 系統。project scope の MCP セットは 3 ツールとも playwright（stdio）と notion（http）の 2 サーバーでそろえる。

- **正本ポリシー**: `.mcp.json` と `.cursor/mcp.json` はそれぞれ独立した設定ファイルとして管理する。片方から他方を自動生成しない
- **変更時ルール**: MCP サーバーの追加・削除・接続先変更をした PR では、`.mcp.json`, `.cursor/mcp.json`, `.codex/config.toml` の整合と本節の更新を同じ PR で行う

#### Claude Code: `.mcp.json`

```json
{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp", "--headless"],
      "env": {}
    },
    "notion": {
      "type": "http",
      "url": "https://mcp.notion.com/mcp"
    }
  }
}
```

#### Cursor: `.cursor/mcp.json`

内容は `.mcp.json` と同一の 2 サーバーを持つ。Cursor 側でも Playwright / Notion を同じ project scope で利用できるようにするための設定。

#### Codex: `.codex/config.toml`

Codex 側では `[mcp_servers.playwright]` と `[mcp_servers.notion]` を `config.toml` に持つ。運用の詳細は [`docs/ops/CODEX_HARNESS.md`](./CODEX_HARNESS.md) を参照。

#### playwright

- **用途**: `ui-designer` / `test-automator` からの interactive な browser 操作。`mcp__playwright__*` ツール群が提供される
- **承認**: 初回呼び出し時に MCP 承認プロンプトが出る。ユーザ承認後にツールが有効化される
- **相互参照**: [`docs/testing/UI_PLAYWRIGHT_VERIFICATION.md`](../testing/UI_PLAYWRIGHT_VERIFICATION.md)

#### notion

- **用途**: Notion Prompt Registry と参考 ES Database の取得。prompt 同期 (`npm run prompts:sync`) や参考 ES 取り込みで raw JSON 取得に使う
- **type**: http（Notion 公式 MCP エンドポイント）
- **認証**: 初回接続時に Notion OAuth フローが走る。個人の Notion アカウントで承認する
- **相互参照**: [`docs/setup/MCP_SETUP.md`](../setup/MCP_SETUP.md) の「Notion 参考 ES の取り込み」「Notion Prompt Registry の同期」節

### 6.2 user scope: `~/.claude/settings.json`（リポジトリ外）

**設定内容**: context7 MCP（`mcp__context7__*` ツール群）。

- **用途**: 全プロジェクト共通で Claude / OpenAI / FastAPI / Next.js / Drizzle などライブラリドキュメントの注入
- **管理**: 個人環境が正本。リポジトリでは設定 JSON を管理しない
- **導入・再設定**: 個別の環境で `~/.claude/settings.json` を編集する。リポジトリへ commit しない
- **外部参照**: https://code.claude.com/docs/en/mcp

### 6.3 導入しない MCP と理由

次の MCP サーバーはあえて導入していない。理由は CLI で代替可能だから。

| サーバー | 代替手段 |
|---|---|
| Supabase MCP | `supabase` CLI（`docs/ops/CLI_GUARDRAILS.md` の許可操作に沿う） |
| Railway MCP | `railway` CLI + `scripts/release/*` |
| Vercel MCP | `vercel` CLI + `scripts/release/*` |
| GitHub MCP | `gh` CLI（`gh pr create` / `gh pr merge` は release scripts 経由） |
| Stripe MCP | `stripe` CLI + webhook ローカル listen |

agent system prompt から CLI を直接叩く方が、権限モデルと監査ログが既存の CLI Guardrails で完結するため、現時点では MCP 化しない判断。将来追加するかは使用頻度と代替 CLI の限界で判断する。

### 6.4 MCP を追加・変更する手順

- **project scope**（リポジトリ共通）:
  1. `.mcp.json`, `.cursor/mcp.json`, `.codex/config.toml` の該当箇所を同じ PR で更新する
  2. 本ドキュメント 6.1 の節を更新する
  3. 初回起動時に承認プロンプトが出る。承認する
- **user scope**（個人環境）:
  1. `~/.claude/settings.json` の `mcpServers` に server を追記する
  2. リポジトリには commit しない
  3. 本ドキュメント 6.2 の節には「存在と用途」のみ反映する

### 6.5 意図的に未導入の改善

- **opus → sonnet 再評価**: 現時点では `opus` の割当を維持する。品質劣化計測の dogfood 設計が別途必要
- **HTTP hook**: 通知先（Slack / Discord）が未整備のため導入しない
- **Prompt hook**: 大規模ファイル編集前の LLM 判定は有用だが、常時コスト増を伴うため現時点では保留

---

## 7. 運用ガイド

### 7.1 agent 委譲の判断

| 状況 | 対応 |
|---|---|
| routing 表にマッチ、影響範囲 2 ファイル以上 | 該当 agent に委譲 |
| routing 表にマッチ、影響範囲 1 ファイル未満、リスク低 | 直接編集可 |
| 複数領域にまたがる、責務境界を跨ぐ | `architect` 起点、`architecture-gate` で判定してから分解 |
| PR 前の品質審査 | `code-reviewer` |
| auth / billing / webhook / csrf の変更 | `security-auditor` を必ず併用 |
| docs-only / test-only / 局所文言 / 局所バグ | 委譲省略可 |

### 7.1.1 Codex 実装委譲（オーケストレーター運用）

正本: `CLAUDE.md` Section C。Claude = オーケストレーター / Codex = ワーカーの閉ループ運用。

**委譲閾値**: ≥3 ファイル or ≥50 行 → Codex 委譲。それ以下 → Claude 直接実装。
**委譲禁止**: 設計判断、セキュリティ、プロンプト、release/deploy、secrets。

**Plan mode での委譲判断**: Section A（設計レビュー）の Codex plan review 完了後、AskUserQuestion で実装委譲の可否を確認する。委譲スコープ・推奨エージェント・コンテキスト準備計画・推定時間・委譲戦略を提示し、ユーザーが選択する。

**閉ループ**:
1. Claude がリサーチ・コンテキスト準備（対象コード + パターン例 + ライブラリ docs + テスト期待値）
2. Codex implementation（workspace-write sandbox）
3. Claude が検証（tsc, unit test, diff review）
4. 不合格 → Codex に1回再委譲（フィードバック付き）→ それでも不合格 → Claude 修正
5. Codex post_review（code-reviewer + security）
6. Section B-2（stage → E2E → commit → push）

**並列安全性**: Codex 実行中は Claude はファイル編集しない。

### 7.2 skill の自律起動が動かないときの診断

1. description マッチを阻害する typo が `.claude/skills/<name>/SKILL.md` に入っていないか確認する
2. 他の skill との trigger 衝突がないか、description 文を比較する
3. `node scripts/agent-pipeline/sync-pipeline.mjs` を走らせて mirror が stale でないか確認する
4. canonical (`private/agent-pipeline/skills/*.md`) が正しいか確認する
5. Claude Code を再起動して session-orientation hook のコンテキストを更新する

### 7.3 ハーネス変更の PR チェックリスト

- [ ] canonical → generator → mirror の順で commit されている
- [ ] `.claude/skills/*.md`, `.codex/**/*.md`, generator 管轄の `.cursor/rules/*.mdc` を手編集していない（例外: 1.2.1 の non-canonical skill と `career-compass-core.mdc`）
- [ ] `AGENTS.md` と `CLAUDE.md` が byte-identical（`diff AGENTS.md CLAUDE.md` が空）
- [ ] `.claude/hooks/*.sh` を変更した場合、5.4 の dry-run が通る
- [ ] `.mcp.json`, `.cursor/mcp.json`, `.codex/config.toml` を変更した場合、6.1 が更新されている
- [ ] `docs/ops/AI_HARNESS.md` の関連節と「最終確認日」が更新されている
- [ ] `npm run test:agent-pipeline` が通る
- [ ] 件数（agent / hook / skill）を変更した場合、`scripts/agent-pipeline/sync-pipeline.test.mjs` の `EXPECTED_HARNESS_INVENTORY` と §7.4 の数値を同じ commit で更新している

### 7.4 検証コマンド

```bash
# AGENTS.md と CLAUDE.md の同期
diff AGENTS.md CLAUDE.md   # 差分なし

# 件数ドリフト確認（2026-04-18 現在の期待値）
# この期待値は sync-pipeline.test.mjs の EXPECTED_HARNESS_INVENTORY と揃える
ls .claude/agents | wc -l     # 13
ls .claude/skills | wc -l     # 41
ls .agents/skills | wc -l     # 19
ls .claude/commands | wc -l   # 11
ls .claude/hooks | wc -l      # 14（top-level 13 sh + lib/）
find .claude/hooks -maxdepth 1 -name '*.sh' -type f | wc -l   # 13
ls .cursor/rules | wc -l      # 29
jq '.mcpServers | keys' .mcp.json   # ["notion","playwright"]
jq '.mcpServers | keys' .cursor/mcp.json   # ["notion","playwright"]
rg "^\[mcp_servers\." .codex/config.toml   # playwright, notion

# settings.json の hook 定義 ↔ 実スクリプト対応
jq -r '.hooks | to_entries[] | .value[] | .hooks[].command' .claude/settings.json

# symlink / manual 例外の確認
ls -la .claude/skills/{ai-product,better-auth-best-practices,security-review,frontend-design,codex-review}
ls -la .cursor/rules/career-compass-core.mdc

# sync-pipeline
node scripts/agent-pipeline/sync-pipeline.mjs
npm run test:agent-pipeline
```

### 7.5 Auto Commit & E2E Verify Workflow

機能実装完了後にコミット → E2E テスト → プッシュを自動化するワークフロー。`CLAUDE.md` Section B-2 が正本。

**順序**: `git add` → E2E スコープ判定 → E2E テスト実行 → (Codex post_review) → `git commit` → `git push`

**依存インフラ**:

| コンポーネント | パス |
|---|---|
| pre-commit gate | `.githooks/pre-commit` → `scripts/git-hooks/enforce-local-ai-e2e.mjs` |
| E2E スコープ解決 | `src/lib/e2e-functional-scope.mjs` |
| 機能→テスト SSOT | `src/lib/e2e-functional-features.mjs` |
| スナップショット | `scripts/ci/e2e-functional-snapshot.mjs` |
| テスト実行 | `make test-e2e-functional-local AI_LIVE_LOCAL_FEATURES={csv}` |
| マニフェスト | `backend/tests/output/local_ai_live/status/{feature}.json` |

**`AI_E2E_AUTO_CONFIRM=1` 環境変数**:

`settings.local.json` で opt-in 設定する。pre-commit hook が非インタラクティブ環境でもテストを自動実行できるようにするセーフティネット。

- **前提条件**: Next.js + FastAPI ローカルサーバー起動済み、DB 接続可能、外部 API key 設定済み
- **コスト**: テスト実行に API 呼び出しが発生する feature あり（LLM 系）
- **退避手段**: `AI_E2E_AUTO_RUN=0` を設定すると hook はテストを実行せずブロックのみ行う

### 7.6 トラブルシューティング

| 症状 | 対応 |
|---|---|
| skill が自律起動しない | 7.2 を順番に確認。generator 未実行が最も多い |
| hook が発火しない | `chmod +x .claude/hooks/*.sh`、`.claude/settings.json` の matcher を確認 |
| hook が誤検知する | 5.4 の dry-run で stdin JSON を再現して原因を特定 |
| MCP ツールが見えない | Claude Code の MCP 承認プロンプトを承認済みか確認、`.mcp.json` の JSON 構文エラーを確認 |
| session-orientation が stale | Claude Code を再起動、`git` が PATH にあるか確認 |
| 一時的に hook を無効化したい | `.claude/settings.json` から該当 entry を外すか、該当 script を `exit 0` のみに差し替える（commit しない） |

---

## 8. ハーネス観測性

ハーネス（Claude Code / Codex 運用層）のみを対象にする。アプリ本体（Next API, FastAPI）のログは各サービスの観測基盤を参照。

### 8.1 agent 呼出頻度の集計

```bash
awk '{print $3}' docs/ops/agent-usage.log | sort | uniq -c | sort -rn | head
```

- データソース: `SessionStart` / `SubagentStart` で発火する `subagent-start-log.sh` が JSONL 互換の行（`TIMESTAMP session_id agent_type agent_id`）を append
- 用途: 自律委譲が routing 表どおり動いているかの俯瞰。特定 agent が異常に多い / 全く呼ばれない場合は description トリガや routing 表との齟齬を疑う

### 8.2 件数ドリフトと Context7 coverage

```bash
npm run test:agent-pipeline
```

- 内部で agent 数 / top-level hook 数 / `lib/skill-recommender.sh` の存在 / opus・sonnet 配分 / 全 agent の Context7 参照 / `.claude/skills`・`.agents/skills` の件数 / 壊れた symlink 0 件 を assert
- 意図的に構成を変える場合は `sync-pipeline.test.mjs` の `EXPECTED_HARNESS_INVENTORY` と §7.4 の数値を同じ commit で更新する

### 8.3 session 単位のコスト

- Claude Code 内で `/cost` コマンド、または statusline の `cost.total_cost_usd` を参照
- 集計の永続化は未実装（v5 候補）

### 8.4 tool 失敗の傾向

```bash
test -f ~/.claude/sessions/career_compass/tool-failure-count && cat $_
```

- `post-tool-failure-triage.sh` が session ごとに tool 失敗件数を累積
- 同 session で多発したときは hook が additionalContext で次アクションを注入する

### 8.5 対象外

アプリ本体のリクエスト／AI 推論のログはハーネスの責務ではない。関連する観測基盤は以下を参照。

- Next API: Vercel 運用ダッシュボード / `src/lib/observability/`
- FastAPI: Railway logs / `backend/app/utils/` 内のロガー
- Stripe / Auth / Calendar: 各プロバイダコンソール

---

## 9. 履歴と参考リンク

- [`docs/review/2026-04-09-claude-code-harness-redesign.md`](../review/2026-04-09-claude-code-harness-redesign.md) — ハーネス再設計の一次記録
- [`AGENTS.md`](../../AGENTS.md) / [`CLAUDE.md`](../../CLAUDE.md) — Subagent Routing 正本
- [`docs/ops/AI_AGENT_PIPELINE.md`](./AI_AGENT_PIPELINE.md) — pipeline の思想と呼び出し方
- [`docs/ops/AI_DEVELOPMENT_PRINCIPLES.md`](./AI_DEVELOPMENT_PRINCIPLES.md) — AI 継続開発の設計原則
- [`docs/ops/CLI_GUARDRAILS.md`](./CLI_GUARDRAILS.md) — CLI ガードレール
- [`docs/ops/SECURITY.md`](./SECURITY.md) — セキュリティベースライン
- [`docs/architecture/FRONTEND_UI_GUIDELINES.md`](../architecture/FRONTEND_UI_GUIDELINES.md) — UI 変更 workflow
- [`docs/testing/UI_PLAYWRIGHT_VERIFICATION.md`](../testing/UI_PLAYWRIGHT_VERIFICATION.md) — UI 変更後の Playwright 検証
- Claude Code 公式: [Hooks Guide](https://code.claude.com/docs/en/hooks-guide), [Skills](https://code.claude.com/docs/en/skills), [MCP](https://code.claude.com/docs/en/mcp), [Sub-agents](https://code.claude.com/docs/en/sub-agents)
