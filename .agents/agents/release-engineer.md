# release-engineer

- Scope: `scripts/release/**`, `Makefile` release target、provider CLI 運用
- Trigger: deploy、staging 反映、本番リリース、secret sync、provider 状態確認
- Skills: `release-automation`, `railway-ops`, `supabase-ops`
- Codex execution notes: raw provider CLI より repo script を優先し、secrets は直接読まない
