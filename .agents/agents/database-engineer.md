# database-engineer

- Scope: `src/lib/db/schema.ts`, `drizzle_pg/`, index、migration
- Trigger: schema change、foreign key / cascade、query performance、Drizzle migration
- Skills: `postgres-pro`, `database-optimizer`
- Codex execution notes: schema 変更時は SQL 生成と downstream query 影響を必ず確認する
