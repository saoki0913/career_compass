1. Deadline status の正本 use case を 1 箇所に寄せ、task side effect と approval state を明示する。
2. Billing は success-only-consumption を feature policy interface へ寄せ、Interview の inline reserve/confirm を同じ語彙で扱う。
3. Conversation は DB JSON / transport / UI state の adapter 境界を Motivation、Gakuchika、Interview で揃える。
4. RAG ingest は source registry、job status、vector/BM25 metadata、GET backfill の責務を分ける。
