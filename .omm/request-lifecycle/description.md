2026-05-07 時点の request lifecycle。Browser は React hooks/components から Next route handler または BFF route を呼び、Next 側が user / guest identity と owner check を解決する。AI/RAG 処理は FastAPI internal endpoint に principal token 付きで委譲し、成功結果だけを Postgres に保存する。Clean Architecture 上は Next route handler が interface adapter、`src/bff` / feature policy が application use case、`src/features` / shared contracts が domain projection、Drizzle/FastAPI/Stripe/LLM が外側の driver になるべきだが、一部 route が application use case と infrastructure 処理を直接抱えている。
