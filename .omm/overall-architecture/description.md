2026-05-07 時点の Career Compass の全体構成。Next.js surface は画面と client state、Next API/BFF は identity・owner check・billing・FastAPI proxy、FastAPI は AI/RAG enrichment、Postgres は transactional state、RAG storage は Chroma/BM25/search を担当する。Clean Architecture 上の理想は shared domain / policy を内側に置き、Next route・FastAPI router・Stripe・DB・LLM・Chroma を外側の adapter / driver として扱うこと。ただし現状は、一部の route handler、UI component、RAG module、AI orchestrator に use case と外部依存が混在している。
