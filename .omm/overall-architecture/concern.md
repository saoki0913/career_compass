Clean Architecture 移行は進んでいるが、境界はまだ安定していない。特に `src/app/api/companies/[id]/fetch-corporate/route.ts` や `fetch-info/route.ts` は HTTP adapter を超えて use case、課金、永続化、backfill を抱える。`backend/app/services/es_review/orchestrator.py` と `backend/app/rag/vector_store.py` は application service と infrastructure の責務が混ざり、変更前に読む量が大きい。UI では `src/components/es/ReviewPanel.tsx` が presentation と use case orchestration を同時に持つ。
