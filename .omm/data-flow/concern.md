状態主体の分散が最大の懸念。Motivation / Gakuchika / Interview は DB JSON、FastAPI payload、TS domain projection、UI hook state が同時に存在する。Deadline は statusOverride、completedAt、tasks.status、autoCompletedTaskIds が複数 route で更新される。Corporate RAG は `companies.corporateInfoUrls` JSON、PDF ingest jobs、Chroma/BM25 metadata、FastAPI status が横断し、backfill も GET route 内にある。状態遷移の正本を domain/use case に寄せないと、継ぎ足し開発で破綻しやすい。
